Electricity prices for EE using elering's open API, the script refreshes it self every 15min to keep everything up to date.

Make sure you install python webview: pip install pywebview

Run the script as normal
